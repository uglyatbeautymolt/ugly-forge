# ugly-forge — benötigte Umgebungsvariablen
# Kopiere diese Datei nach .env.forge und fülle die Werte aus
# NIEMALS .env.forge committen!

# GitHub
GITHUB_TOKEN=
GITHUB_USERNAME=

# GPG — für projekt-spezifische .env.gpg Verschlüsselung
# Ein Key für alle Projekte
PROJEKT_GPG_KEY=

# OpenRouter — bereits in ugly-stack vorhanden
# Wird von dort übernommen, hier nicht nötig

# Telegram — bereits in ugly-stack vorhanden  
# Wird von dort übernommen, hier nicht nötig
